function operations() {
    const calc = document.querySelector('.container-form');
    const inputs_ = document.querySelector('.inputs');

    inputs_.addEventListener('click', e => {if (e.target.matches('button')) {}})
}